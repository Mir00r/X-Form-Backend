{
  "name": "xform-realtime-service",
  "version": "1.0.0",
  "description": "Real-time WebSocket service for X-Form Backend",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "demo": "echo 'Opening demo client...' && open demo/websocket-demo.html || echo 'Open demo/websocket-demo.html in your browser'",
    "docs": "echo 'API documentation available at: http://localhost:8002/api-docs'",
    "health": "curl -s http://localhost:8002/health | json_pp || echo 'Service not running'",
    "stats": "curl -s http://localhost:8002/stats | json_pp || echo 'Service not running'"
  },
  "dependencies": {
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "redis": "^4.6.10",
    "socket.io": "^4.7.4",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "X-Form Team",
  "license": "MIT"
}
